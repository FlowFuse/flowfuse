const initialState = () => ({})

const meta = {
    persistence: {}
}

const state = initialState

const getters = {}

const mutation = {}

const actions = {}

export default {
    namespaced: true,
    meta,
    state,
    getters,
    mutations: mutation,
    actions
}
