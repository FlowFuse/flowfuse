// TODO: Remove this module shell once product-expert (PR 12) is merged.
// All state/actions have been migrated to Pinia stores.

const initialState = () => ({})

const state = initialState

const getters = {}

const mutations = {}

const actions = {}

export default {
    namespaced: true,
    modules: {},
    state,
    initialState: initialState(),
    getters,
    actions,
    mutations
}
