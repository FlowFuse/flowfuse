// The first nr-assistant release that supports agent-driven flow building inside the
// Node-RED editor. An editor running an older nr-assistant may still load fine, but
// cannot be driven by an agent, so tools that talk to the editor gate on this.
export const MIN_NR_ASSISTANT_VERSION_FOR_AGENT_FLOW_BUILDING = '0.16.0'
