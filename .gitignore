/node_modules/
forge/containers/localfs_root
frontend/dist
frontend/dist-setup
forge.db
docs/jsdoc

# Users should have a system based .gitignore that works globally, however, this
# list is a failsafe when they don't.
.DS_Store
